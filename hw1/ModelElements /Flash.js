class Flash {
  constructor(location, angle, color, power) {
    this.location = location;
    this.angle = angle;
    this.color = color;
    this.power = power;
  }

  rotate(grad) {
    // Реализация метода для вращения
    // использовать переданный угол и изменить угол молнии
  }

  move(cm) {
    // Реализация метода для перемещения
    // использовать переданные данные расстояния и изменить положение молнии
  }
}
